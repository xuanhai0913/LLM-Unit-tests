FROM node:18-alpine

# Install testing frameworks globally
RUN npm install -g jest mocha chai

# Set NODE_PATH so required modules can be found
ENV NODE_PATH=/usr/local/lib/node_modules

WORKDIR /app

CMD ["npm", "test"]
